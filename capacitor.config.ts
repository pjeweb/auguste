import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.alzhmeimerniedersachsen.auguste',
  appName: 'Auguste',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
